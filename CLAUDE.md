# CHA_SCAN — epiSCANEER 작업 규칙

이 리포지토리에서 epiSCANEER 분석을 수행할 때 반드시 지킬 규칙.
아래 규칙은 편의나 실행 시간을 이유로 완화할 수 없다.

## 1. 방법론은 논문을 그대로 따른다

기준 논문: **epiSCANEER, Nucleic Acids Research 2026, 54, gkag879**
("Enhancing prediction accuracy for enzyme activity engineering through
sequence co-evolution and epistatic relationship modeling")

Methods "MSA generation" 원문 그대로가 유일한 기준이다:

- 상동서열 검색: **UniRef90** 데이터베이스, **PSI-BLAST**
- PSI-BLAST 설정: **단일 iteration**, **e-value 임계값 0.001**
- 길이 필터: 쿼리 길이의 **0.7 ~ 1.3배**만 유지
- 동일성 필터: 쿼리와 **90% 이상 동일한 서열은 제외**
- 정렬: **MUSCLE**, ClustalW 출력 형식, **기본 파라미터**
- 서열 개수 상한/하한: **없음.** 필터를 통과한 전부를 사용한다.
  (논문 실적: KGSADH 250, LdhA 249, TetA 57 — 모두 emergent 값)

## 2. 금지 사항

- **기준을 임의로 만들지 않는다.** 논문에 없는 필터(상호 동일성 컷,
  커버리지 컷, 절대 길이 범위 등)를 추가하지 않는다.
- **개수 상한(cap)을 두지 않는다.** `--target`류의 조기 중단을 쓰지 않는다.
- **무작위 표본추출을 하지 않는다.** 시드 기반 셔플 후 앞에서 자르는
  방식은 금지. 결과가 표본 임의성에 의존해서는 안 된다.
- **검색 도구를 대체하지 않는다.** PSI-BLAST 대신 Pfam 패밀리 다운로드,
  pairwise 정렬 점수, k-mer 유사도 등으로 우회하지 않는다.
- **정렬 도구를 대체하지 않는다.** MUSCLE 이외의 정렬기를 쓰지 않는다.

## 3. 막히면 우회하지 말고 보고한다

논문 방법을 그대로 실행할 수 없는 상황(도구 미설치, 디스크 부족,
외부 서비스가 개인정보를 요구함 등)이 발생하면:

1. **대체 방법을 임의로 선택하지 않는다.**
2. 무엇이 왜 막혔는지, 필요한 자원이 무엇인지 사용자에게 보고한다.
3. 사용자의 결정을 받은 뒤에 진행한다.

## 4. 결과 보고 시 필수 고지

- 논문 방법에서 벗어난 부분이 하나라도 있으면 **결과보다 먼저** 명시한다.
- 각 단계의 서열/후보 개수를 실제 로그 수치로 보고한다.
  (다운로드 수, 필터 통과 수, MSA 최종 수 — 추정치 금지)
- epiSCANEER 출력이 아닌 값(구조 거리, 기능 주석 등)을 컬럼에 추가할
  경우, 그것이 외부 주석이며 순위 산정에 미반영임을 명시한다.
- 순위 산정에 개입한 필터가 있으면 제외된 항목 수와 근거를 밝힌다.

## 5. 환경 메모

- Python: `.venv/` (biopython, networkx, numpy, openpyxl)
- Java 21 필요 (McBASC 공변이 계산)
- LibreOffice는 이 컨테이너에서 파일을 로드하지 못함 → xlsx 수식
  재계산 검증 불가. 수식 대신 계산된 값을 쓰고 openpyxl로 재검증한다.
